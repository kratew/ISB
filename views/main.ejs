<!DOCTYPE html>
<html>

<head>
    <% include include/header %>
    <title>Imagine Sensor for the Blind</title>
</head>

<body>
    <% include include/navigator %>
    <div id="main" class="scroll-container">
        <!-- Main Page Section -->
        <section class="section1">
            <img class="sec1bg" src="./img/t​rapezoid.png">
            <div>
                <img src="./img/ISB_verti.png">
                <div>
                    <a href="/signin"><button>SIGN IN</button></a>
                    <a href="/signup"><button>SIGN UP</button></a>
                </div>
            </div>
        </section>
        <!-- About Us Section -->
        <section class="section2">
            <div>
                <div class="leftBox title">
                    <h2>About Us</h2>
                    <div class="pager">
                        <div class="pager-item active">ISB 서비스 개요</div>
                        <div class="pager-item">프로젝트 개요</div>
                    </div>
                </div>
                <div class="rightBox">
                    <ul class="aboutSlide">
                        <li>
                            <div class="keyword">
                                <p class="introduce">
                                    시각 장애인들은 장애를 극복하기 위해 소리가 눈에 보이지 않는 물체로부터 부딪혀 돌아오는 파동을 인지함으로써 자신과 그 물체 사이의 거리를 청각에
                                    의존해 파악한다. <br>
                                    따라서, 초음파 센서를 이용하여 시각 장애인들이 소리가 물체에 부딪혀 돌아오는 파동을 인지하는 과정을 대신 해주고, 거리의 차이에 따라 음의 높낮이를
                                    다르게 하여 시각 장애인과 물체간의 거리감을 인지시켜주는 서비스를 개발한다.<br>
                                    또한, 기존 내비게이션 기능을 함께 활용하여 음성 안내만으로 사용자가 가고자 하는 길을 안내해주는 동시에 보행 시 전방의 위험 지역이나 방해물을
                                    알려주어 시각 장애인들을 위한 보다 안전한 일상 생활을 만들어 줄 수 있는 서비스를 기대할 수 있다.
                                </p>
                            </div>
                        </li>
                        <li class="keytwo">
                            <div class="about2">
                                <p>
                                    주요한 기능 중에는 초음파 센서를 통한 공간 인지, 내비게이션, 음성인식이 있다.<br>
                                    <br>
                                    1. 초음파 센서를 통한 공간 인지 <br>
                                    초음파 센서를 이용해서 주변에 보행에 방해가 되는 사물이나 지상구조물이 있는지 인지할 수 있고, 내비게이션이 가리키는 방향을 벗어나지 않도록 나침반
                                    역할을 하는 안내음을 들려주는 기능을 구현할 예정이다. <br>
                                    <br>
                                    2. 내비게이션 <br>
                                    서버에서 지도 정보를 가져와서 복잡하지 않고 쉽게 찾아갈 수 있는 단일 경로를 뽑아낸 다음, 이를 작은 경로단위로 쪼개서 음성 안내를 해주는 기능을
                                    구현할 예정이다. <br>
                                    <br>
                                    3. 음성인식 <br>
                                    음성으로 사용자의 Voice Key를 만들어서 사용자 인식을 하고, 음성으로 내비게이션에게 명령을 내리는 기능을 구현할 예정이다. <br>
                                </p>
                            </div>
                        </li>
                    </ul>
                </div>
            </div>
        </section>
        <!-- Team section -->
        <section class="sectionTeam">
            <div>
                <div class="leftBox title">
                    <h2 class="White">Team</h2>
                </div>
                <div class="rightBox">
                    <div>
                        <ul>
                            <li><span>김휘림</span><span>가천대학교 컴퓨터공학과</span></li>
                            <li><span>우대철</span><span>가천대학교 컴퓨터공학과</span></li>
                            <li><span>최성원</span><span>가천대학교 컴퓨터공학과</span></li>
                        </ul>
                    </div>
                </div>
            </div>
            <% include include/footer %>
        </section>
    </div>
    <!-- <div id="bg"></div> -->

    <ul class="dots dots-left">
        <li><a href="#1" class="active"></a></li>
        <li><a href="#2"></a></li>
        <li><a href="#3"></a></li>
    </ul>

    <!-- Swiper JS -->
    <script src="./js/jquery-2.1.1.min.js"></script>
    <!-- Initialize Swiper -->
    <script src="./js/swiper.min.js"></script>

    <script>
        $(document).ready(function () {
            // 마우스 휠 이벤트
            $("section").each(function () {
                $(this).on('mousewheel', function (e) {
                    var $this_mouseEvent = $(this);

                    // 이벤트 초기화
                    e.preventDefault();

                    var delta = 0;
                    if (!event) event = window.event;
                    if (event.wheelDelta) {
                        delta = event.wheelDelta / 120;
                        if (window.opera) delta = -delta;
                    } else if (event.detail) delta = -event.detail / 3;
                    var moveTop = null;
                    var idx = $this_mouseEvent.index();
                    var secL = $('section').length;
                    // 마우스휠을 위에서 아래로
                    if (delta < 0) {
                        if ($this_mouseEvent.next().index() < secL && $this_mouseEvent.next().index() != -1) {
                            moveTop = $this_mouseEvent.next().offset().top;
                            // 오른쪽 네비 활성화
                        } else {
                            moveTop = $this_mouseEvent.last().offset().top;
                            // 오른쪽 네비 활성화
                        }
                        // 마우스휠을 아래에서 위로
                    } else {
                        if ($this_mouseEvent.prev().index() >= 0) {
                            moveTop = $this_mouseEvent.prev().offset().top;
                            // 오른쪽 네비 활성화
                        }
                    }
                    // 화면 이동 0.5초(500)
                    $("html, body").stop().animate({
                        scrollTop: moveTop
                    }, {
                            duration: 500, complete: function () { }
                        });
                });
            });

            // 네비 클릭시 이벤트
            $('nav li').click(function () {
                var $section = $('section');
                var $html_body = $('html, body');
                var $dots_li = $('.dots li');
                var $dots_li_a = $dots_li.find('a');

                var idx = $(this).index();
                var secH = $section.height();
                var secP = parseInt($section.css('padding-top'));
                if (idx == 0) {
                    $html_body.animate({ scrollTop: secH * 1 + (secP * 1) });
                    $dots_li_a.removeClass('active');
                    $dots_li.eq(1).find('a').addClass('active');
                } else {
                    $html_body.animate({ scrollTop: secH * (idx + 2) + (secP * (idx + 2)) });
                    $dots_li_a.removeClass('active');
                    $dots_li.eq(idx + 2).find('a').addClass('active');
                }
            });

            // 오른쪽 동그라미 버튼 클릭시 이벤트
            $('.dots li').click(function () {
                var $this = $(this);
                var $section = $('section');

                $('.dots li a').removeClass('active');
                $this.find('a').addClass('active');

                var idx = $this.index();
                var secH = $section.height();
                var secP = parseInt($section.css('padding-top'));

                // console.log(secP);

                $('html, body').animate({ scrollTop: secH * idx + (secP * idx) });
            });

            // about2 li 밑에 버튼 클릭시 이벤트
            $('.pager .pager-item').click(function () {
                var $this = $(this);
                var $aboutSlide = $('.aboutSlide');

                var pagerW = $aboutSlide.width();
                var idx = $this.index();
                $('.pager .pager-item').removeClass('active');
                $this.addClass('active');
                if (idx == 1) {
                    $aboutSlide.animate({ 'margin-left': '-100%' });
                }
                else {
                    $aboutSlide.animate({ 'margin-left': '0' });
                }
            });

            // section1~9까지 offset().top값 반복문
            var scArr = new Array();
            for (var i = 1; i < 4; i++) {
                scArr[i - 1] = $('.section' + i).offset().top;
            }

            // 윈도우 스크롤할 대 섹션당 애니메이션 효과 및 오른쪽 동그라미 활성화
            $(window).on('scroll load', function () {
                var wTop = $(this).scrollTop();
                var $dots_li = $('.dots li');
                var $dots_li_a = $dots_li.find('a');

                if (wTop >= 0 && wTop < scArr[1]) {
                    $dots_li_a.removeClass('active');
                    $dots_li.eq(0).find('a').addClass('active');
                    $('.section1 .mTitle').animate({ 'opacity': '1', 'margin-top': '180px' }, 500);
                } else if (wTop >= scArr[1] && wTop < scArr[2]) {
                    $dots_li_a.removeClass('active');
                    $dots_li.eq(1).find('a').addClass('active');
                } else if (wTop >= scArr[2] && wTop < scArr[3]) {
                    $dots_li_a.removeClass('active');
                    $dots_li.eq(2).find('a').addClass('active');
                    $section3.find('.img2').delay(300);
                    $section3.find('img').animate({ 'opacity': '1', 'top': '52%' }, 300);
                }
            });
        });
    </script>
</body>

</html>